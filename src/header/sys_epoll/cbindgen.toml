# Non-POSIX spec: http://man7.org/linux/man-pages/man7/epoll.7.html
include_guard = "_RELIBC_SYS_EPOLL_H"
after_includes = """
#include <bits/sigset-t.h> // for sigset_t from signal.h
"""
language = "C"
style = "Tag"
no_includes = true
cpp_compat = true

[enum]
prefix_with_name = true
